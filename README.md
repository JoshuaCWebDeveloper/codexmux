# codexmux

Dashboard tool for viewing active codex sessions using tmux.

## Installation

Run `./install.sh` to install the script under `~/.local/bin/codexmux`.

Then, run `codexmux` to start the tool.
